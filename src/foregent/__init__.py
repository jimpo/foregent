"""Foregent — autonomous multi-agent software development system.

This package holds the bridge: an in-memory model of the issues foregent is
working, the agent harness that runs them (:mod:`foregent.agents`), and the
``foregent`` CLI that reports on them. The tick that feeds it events from
Linear and GitHub is not here yet.
"""

__version__ = "0.1.0"
